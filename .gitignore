# Dependencies
node_modules/

# Build cache
.next/

# Environment
.env
.env.local

# SEO Seed agent — stays local, only its generated HTML in out/seed/ gets pushed
seo_seed/

# Local-only prompts (paste into a chat model, not committed)
docs/KEYWORD_BET_PROMPT.md

# OS files
.DS_Store
Thumbs.db
